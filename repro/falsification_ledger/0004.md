# Post 0004 - What Are We Holding Fixed? (Falsification Ledger)

Source post: `content/0004-420-moe-edition.md`
Companion claim ledger: `repro/claim_ledger/0004.md`
Falsification status: `partial_attack`

## Live claim

The fairness contract is a first-class variable in dense-vs-MoE research. `0003` already gives the active-like side; the missing experiment is the `totalDN` side for the same dense / `MoE-64` / `MoE-256` family.

## Formal object -> observable map

- `P_total(m)`: all trainable parameters in model `m`
- `P_active(m)`: canonical active-parameter object from `nmoe.metrics.param_counts`
- `T_total(m) = alpha * P_total(m)`
- `T_active(m) = beta * P_active(m)`
- observables: target-hit step, final `bpb`, final `CORE`, router-health summaries under a fixed speedrun family

## Existing attacks

- the original `d10` / `d12` transfer was exposed as structurally confounded instead of being passed off as a clean scaling law
- `0003` already provides an almost-equal active-parameter comparison at matched active FFN width per token
- the post now cleanly separates the formal objects, the measured surfaces, and the still-open ranking question

## Missing brutal attacks

- rerun `MoE-64` in `bf16` under the exact `totalDN` horizon and scaled schedule: `37813` steps, warmup `1015`, warmdown `8121`
- rerun `MoE-256` in `bf16` under the exact `totalDN` horizon and scaled schedule: `141963` steps, warmup `3811`, warmdown `30489`
- compare the active-like `0003` surface against the new `totalDN` surface at fixed fractions of each contract, not only at target-hit step
- if the `bf16` answer is interesting, repeat the same contract comparison in `fp8`

## Launch gate

- Green for shaping and internal truthfulness: the post now asks the right question and states the exact missing proof surface.
- Red for the strongest public claim: do not claim that the contract choice changes the ranking until the matching `totalDN` sparse reruns exist.
